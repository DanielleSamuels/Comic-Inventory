spring:
  application:
    name: library-service
  cloud:
    config:
      uri: http://config-server:8089
      fail-fast: true
      retry:
        max-attempts: 12
        initial-interval: 1000
        multiplier: 1.5
        max-interval: 5000
