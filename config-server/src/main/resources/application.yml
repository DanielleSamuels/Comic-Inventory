spring:
  application:
    name: config-server
  profiles:
    #active: native
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DanielleSamuels/config.git
          clone-on-start: true
          force-pull: true
        #native:
          #search-locations: classpath:/config

server:
  port: 8089

management.endpoints.web.exposure.include: "*"
