spring:
  application:
    name: gateway-server
  config:
    import: "optional:configserver:http://config-server:8089"
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 12
        initial-interval: 1000
        multiplier: 1.5
        max-interval: 5000